# Shorts - 截图标注工具

> 仿 Mac iShot 功能的 Windows 截图标注工具

## 1. 项目概述

- **项目名称**: Shorts
- **项目类型**: Windows 桌面应用
- **核心功能**: 区域截图 + 即时标注 + 快捷保存
- **目标用户**: 需要频繁截图标注的开发者、产品经理、设计师

## 2. 技术栈

- **语言**: Python 3.14
- **GUI框架**: PyQt6
- **截图库**: mss
- **打包**: PyInstaller

## 3. 功能列表

### 3.1 截图模式

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 区域截图 | 鼠标拖拽选择矩形区域 | P0 |
| 窗口截图 | 点击窗口直接截取 | P1 |
| 全屏截图 | 截取整个屏幕 | P1 |
| 长截图 | 滚动截取整个网页/文档 | P2 |

### 3.2 标注工具

| 工具 | 描述 | 快捷键 |
|------|------|--------|
| 箭头 | 绘制带箭头的线条 | A |
| 矩形 | 绘制矩形框 | R |
| 椭圆 | 绘制椭圆/圆形 | O |
| 文字 | 添加文字标注 | T |
| 画笔 | 自由绘制 | P |
| 高亮 | 半透明高亮区域 | H |
| 马赛克 | 模糊敏感区域 | M |

### 3.3 标注属性

- **颜色**: 红、蓝、黄、绿、黑、白
- **线条粗细**: 2px, 4px, 6px
- **填充**: 有填充/无填充
- **字体大小**: 14px, 18px, 24px

### 3.4 输出方式

- 复制到剪贴板（默认）
- 保存到文件（PNG）
- 拖拽到其他应用

### 3.5 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+Shift+A | 区域截图 |
| Ctrl+Shift+W | 窗口截图 |
| Ctrl+Shift+F | 全屏截图 |

## 4. UI/UX 设计

### 4.1 截图模式

```
┌─────────────────────────────────────────┐
│                                         │
│         屏幕半透明遮罩 (暗色)            │
│                                         │
│     ┌───────────────────────────┐       │
│     │    选区框 (可调整大小)      │       │
│     └───────────────────────────┘       │
│                                         │
│   底部: 工具栏 [区域|窗口|全屏] [取消]  │
└─────────────────────────────────────────┘
```

### 4.2 标注模式

```
┌─────────────────────────────────────────┐
│ ┌──┐                                     │
│ │× │  ← 关闭按钮                          │
│ └──┘                                     │
│                                         │
│          截图内容 + 标注层               │
│        (可拖拽、缩放选区)               │
│                                         │
├─────────────────────────────────────────┤
│ [←][→] │ ↑ ↓ │ | 工具栏: [→箭头][□矩形][○椭圆][T文字][✎画笔][▤高亮][██马赛克] │ [复制] [保存] │
└─────────────────────────────────────────┘
```

### 4.3 视觉风格

- **主题**: 浅色主题，仿 Mac 风格
- **工具栏**: 圆角矩形，浅灰色背景
- **图标**: 简洁线条图标
- **阴影**: 轻微投影，增加层次感

### 4.4 交互流程

1. **触发截图** → 全屏显示半透明遮罩
2. **选择区域** → 鼠标拖拽生成选区
3. **进入标注** → 弹出标注工具栏
4. **添加标注** → 在截图上绘制
5. **完成输出** → 复制/保存/拖拽

## 5. 项目结构

```
shorts_app/
├── shorts_app.py          # 入口文件
├── requirements.txt       # 依赖
├── shorts_app.spec        # PyInstaller 配置
├── core/
│   ├── screenshot.py      # 截图核心逻辑
│   ├── annotation.py      # 标注引擎
│   └── hotkey.py          # 全局热键
├── app/
│   ├── ui/
│   │   ├── capture_overlay.py   # 截图遮罩窗口
│   │   ├── annotation_window.py # 标注编辑窗口
│   │   └── toolbar.py           # 工具栏
│   └── widgets/
│       ├── color_picker.py      # 颜色选择器
│       └── annotation_item.py   # 标注元素
└── resources/
    └── icons/             # 图标资源
```

## 6. 验收标准

### 功能验收

- [ ] 区域截图：可拖拽选择矩形区域
- [ ] 窗口截图：点击窗口获取区域
- [ ] 全屏截图：截取全部屏幕
- [ ] 箭头标注：可调整方向和颜色
- [ ] 矩形标注：可填充/描边
- [ ] 椭圆标注：可填充/描边
- [ ] 文字标注：可编辑文字和颜色
- [ ] 画笔标注：自由绘制
- [ ] 高亮标注：半透明效果
- [ ] 马赛克：模糊区域
- [ ] 复制到剪贴板
- [ ] 保存到文件
- [ ] 全局快捷键触发

### 体验验收

- [ ] 截图响应 < 500ms
- [ ] 标注操作流畅
- [ ] 工具栏直观易用
- [ ] 快捷键全局生效